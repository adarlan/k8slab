on:
  push:
    branches:
      - ${{ github.event.repository.default_branch }}
    # paths:
    #   - kubernetes-cluster/eks-cluster
    #   - .github/workflows/.terraform-module.yaml
    #   - .github/workflows/eks-cluster.yaml
  pull_request:
    branches:
      - ${{ github.event.repository.default_branch }}
    # paths:
    #   - kubernetes-cluster/eks-cluster
    #   - .github/workflows/.terraform-module.yaml
    #   - .github/workflows/eks-cluster.yaml

jobs:
  eks-cluster:
    uses: adarlan/k8slab/.github/workflows/.terraform-module.yaml@master
    with:
      context_path: kubernetes-cluster/eks-cluster
