apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}

spec:
  selector:
    app: {{ .Release.Name }}

  ports:
  - name: web
    port: {{ .Values._servicePortNumber }}
    protocol: TCP
    targetPort: web

  - name: metrics
    port: 9100
    protocol: TCP
    targetPort: metrics
